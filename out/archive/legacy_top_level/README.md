# Legacy top-level outputs

These files occupied the root of `out/` before the 8 September 2026 cleanup.
`path_map.json` preserves their original locations. They are historical
exploratory outputs, not the current manuscript analysis. Aggregate tables are
versioned; individual predictions and legacy interactive figures stay local.
The initial September audit describes their locations before this move.
